# AWS Configuration
AWS_REGION=us-east-1
DB_SECRET_NAME=MARILYN_MONROE

# Security
SECRET_KEY=dev-secret-key-change-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Google Gemini API (for AI agents and web search)
GOOGLE_API_KEY=why-are-you-reading-this

# OpenAI API (for Whisper voice transcription)
OPENAI_API_KEY=why-do-you-continue-to-read-it

# Opik by Comet (for LLM tracing and evaluation)
OPIK_API_KEY=what-do-you-think-will-happen
OPIK_WORKSPACE=default
OPIK_PROJECT_NAME=neuroresolv

# Application Settings
ENVIRONMENT=development
DEBUG=true

# Local Database Settings (Used when ENVIRONMENT=development)
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=neuroresolv

# CORS Origins (for frontend)
CORS_ORIGINS=["http://localhost:3000","http://localhost:5173","https://neuro-resolv.vercel.app"]
